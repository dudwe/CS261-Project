Made using PHP v7.2.2, MySQLi, and MySQLv15.1 (10.1.31-MariaDB)

To view the database, login using `mysql -u root`, then `use database traderbot_db;` at the prompt.
Use `show tables` to see the tables in the database, and `select * from <table>` to see the stuff in a table.
